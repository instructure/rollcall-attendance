#!/bin/bash

## Boot the app
if [ "$ROLLCALL_ROLE" == "worker" ]; then
  echo "Starting worker"
  exec COUNT=${WORKER_COUNT:-1} bundle exec rake resque:workers
else
  echo "Starting web server"
  exec /usr/src/entrypoint
fi
